{
  "name": "ipynb-to-md",
  "displayName": "IPYNB to MD Converter",
  "description": "Convert Jupyter Notebook (.ipynb) files to Markdown (.md) files on save using Jupytext",
  "version": "0.0.1",
  "publisher": "your-publisher-name",
  "engines": {
      "vscode": "^1.50.0"
  },
  "activationEvents": [
      "*"
  ],
  "main": "./out/extension.js",
  "scripts": {
      "vscode:prepublish": "npm run compile",
      "compile": "tsc -p ./"
  },
  "devDependencies": {
      "@types/vscode": "^1.50.0",
      "@types/node": "^14.14.6",
      "typescript": "^4.0.3"
  },
  "dependencies": {},
  "contributes": {}
}